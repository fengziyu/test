---
kind: pipeline
type: docker
name: default

steps:
  - name: pubilsh
    image: plugins/docker
    volumes:
      - name: docker.sock
        path: /var/run/docker.sock
    settings:
      repo: hub.fengziyu.top/vue
      tags: latest

volumes:
  - name: docker.sock
    host:
      path: /var/run/docker.sock
